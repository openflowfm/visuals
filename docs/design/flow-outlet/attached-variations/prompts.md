# Attached-control variations

Generated with the built-in image-generation tool using c-attached-controls.png as reference. Static appearance studies only. User requested resumed variations after recording intent; no selection implied.

The continuous-edge study explores strongest material unity. Images retain more persistent labeling and heavy outlines than the intended quiet overview. Cable placement is illustrative and does not validate fixed anchoring across hover states. These details require correction before implementation.

## 01 / Control tab

Use case: ui-mockup. Create a high resolution 3:2 landscape UX concept sheet for visual[flow]. Use the attached "C / Attached controls" image as an INTERACTION REFERENCE, NOT an instruction to copy its glossy hardware styling. Keep its defining feature: actual parameter adjustments sit INLINE on small breakouts attached to their node. Inputs on left, output on right; connection anchors and node positions stay fixed across hover. At rest tiny icon and label only when needed, no parameter lists. On hover or keyboard focus just that breakout reveals its control; dragging holds it open. This is a real 2D APPLICATION interface, NOT a physical render: no chrome, thick glass, reflection floor, 3D perspective, lab vessels, neon tubes, clunky sockets, or detached inspector. Some subtle gloss is welcome via a soft top surface highlight. Node surfaces charcoal, preview contents cyan/violet. Pulse source is a soft amber light varying in intensity, NOT a waveform. Wires thin violet picture signal and amber numeric signal, restrained glow at most. Type pictograms help without relying on color alone.
Layout: ivory editorial sheet, modest black title and one short subtitle, dominant dark canvas hero with source checker left, ripple Lens center-right, smaller amber pulse source below-left. Result is shown inside Lens itself; no extra output node needed, no app navigation or surrounding chrome. Source violet picture wire terminates at Lens left Picture breakout; Pulse amber wire terminates at Lens left Depth breakout. Flow jack on right Lens edge. Preview first. Main scene shows Depth hovered and its inline adjustment visible. Bottom 3 evenly spaced closeups of SAME node and same breakout geometry "At rest", "Hover or focus", "Adjusting". A single-control connected input editor labeled "Depth range" with dual-ended tiny track and values "10%" and "35%"; adjusted version "10%" and "22%". This is a proposed range UI, not a source-signal slider. The incoming cable must continue to the SAME socket visible at the same relative position in every state; the control extension opens perpendicular to or alongside anchor, never shifts socket. Keep main node and breakout visibly unified. Show no controls on any other node while this breakout is hovered. Small footer "One correction. Everything else stays put." No extra paragraphs or tiny filler text. Generous whitespace and crisp UI-sized typography, enough closeup scale to judge design. Exactly one sheet, no side-by-side whole proposals.Title "01 / Control tab". Subtitle "A small adjustment at the end of the wire." The most compact interpretation. Main nodes refined flat dark rectangles with 8px rounded corners, thin neutral edge, slight digital surface sheen, rich crisp previews. Left Depth inlet lives on a very short small rounded tab attached flush to node; single tiny amber socket at its OUTER left tip. On hover a narrow control wing unfolds DOWNWARD from this tab, contiguous same fill and edge as parent; "Depth range" at top, numeric endpoints beneath thin track. Socket remains above control wing, cable routes through free canvas so it NEVER intersects track. At rest that wing is absent and the tab only shows a simple modulation pictogram and "Depth" on hover/focus, a subtle static range mark at rest. No big slider card floating to left. Control wing is noticeably smaller than node, like a small attached flap, not a second node. The amber pulse generator is tiny square with warm diffused light and a minuscule sine pictogram near outlet. Overall look: restrained professional creative software.

## 02 / Continuous edge

Use case: ui-mockup. Create a high resolution 3:2 landscape UX concept sheet for visual[flow]. Use the attached "C / Attached controls" image as an INTERACTION REFERENCE, NOT an instruction to copy its glossy hardware styling. Keep its defining feature: actual parameter adjustments sit INLINE on small breakouts attached to their node. Inputs on left, output on right; connection anchors and node positions stay fixed across hover. At rest tiny icon and label only when needed, no parameter lists. On hover or keyboard focus just that breakout reveals its control; dragging holds it open. This is a real 2D APPLICATION interface, NOT a physical render: no chrome, thick glass, reflection floor, 3D perspective, lab vessels, neon tubes, clunky sockets, or detached inspector. Some subtle gloss is welcome via a soft top surface highlight. Node surfaces charcoal, preview contents cyan/violet. Pulse source is a soft amber light varying in intensity, NOT a waveform. Wires thin violet picture signal and amber numeric signal, restrained glow at most. Type pictograms help without relying on color alone.
Layout: ivory editorial sheet, modest black title and one short subtitle, dominant dark canvas hero with source checker left, ripple Lens center-right, smaller amber pulse source below-left. Result is shown inside Lens itself; no extra output node needed, no app navigation or surrounding chrome. Source violet picture wire terminates at Lens left Picture breakout; Pulse amber wire terminates at Lens left Depth breakout. Flow jack on right Lens edge. Preview first. Main scene shows Depth hovered and its inline adjustment visible. Bottom 3 evenly spaced closeups of SAME node and same breakout geometry "At rest", "Hover or focus", "Adjusting". A single-control connected input editor labeled "Depth range" with dual-ended tiny track and values "10%" and "35%"; adjusted version "10%" and "22%". This is a proposed range UI, not a source-signal slider. The incoming cable must continue to the SAME socket visible at the same relative position in every state; the control extension opens perpendicular to or alongside anchor, never shifts socket. Keep main node and breakout visibly unified. Show no controls on any other node while this breakout is hovered. Small footer "One correction. Everything else stays put." No extra paragraphs or tiny filler text. Generous whitespace and crisp UI-sized typography, enough closeup scale to judge design. Exactly one sheet, no side-by-side whole proposals.Title "02 / Continuous edge". Subtitle "The node edge becomes the control." A more integrated approach. Main nodes thin subtly translucent charcoal rounded rectangles with image nearly edge-to-edge. The Picture and Depth inlets are part of a gently stepped LEFT silhouette, not separate pills. Each step contains an icon-sized socket, no visible seam separating from node body. On hover the Depth step develops a slim sideways shelf immediately BELOW its fixed socket; same continuous outer contour and shared backdrop. Range track lies horizontally on shelf with exact "Depth range", "10%" and "35%" markings. It must visually read as the node stretching locally, not a popup or a second box. The stepped shape has precise designed curvature, not a blob. On rest the contour is compact. On hover range shelf uses empty area below incoming cable; main preview unchanged. Fine violet and amber current, no glowing outlines. Main pulse node small rectangular patch of softly breathing light, icon-only. Three closeups clearly communicate evolving SINGLE silhouette with fixed input socket. Most architecturally unified variant.

## 03 / Soft neck

Use case: ui-mockup. Create a high resolution 3:2 landscape UX concept sheet for visual[flow]. Use the attached "C / Attached controls" image as an INTERACTION REFERENCE, NOT an instruction to copy its glossy hardware styling. Keep its defining feature: actual parameter adjustments sit INLINE on small breakouts attached to their node. Inputs on left, output on right; connection anchors and node positions stay fixed across hover. At rest tiny icon and label only when needed, no parameter lists. On hover or keyboard focus just that breakout reveals its control; dragging holds it open. This is a real 2D APPLICATION interface, NOT a physical render: no chrome, thick glass, reflection floor, 3D perspective, lab vessels, neon tubes, clunky sockets, or detached inspector. Some subtle gloss is welcome via a soft top surface highlight. Node surfaces charcoal, preview contents cyan/violet. Pulse source is a soft amber light varying in intensity, NOT a waveform. Wires thin violet picture signal and amber numeric signal, restrained glow at most. Type pictograms help without relying on color alone.
Layout: ivory editorial sheet, modest black title and one short subtitle, dominant dark canvas hero with source checker left, ripple Lens center-right, smaller amber pulse source below-left. Result is shown inside Lens itself; no extra output node needed, no app navigation or surrounding chrome. Source violet picture wire terminates at Lens left Picture breakout; Pulse amber wire terminates at Lens left Depth breakout. Flow jack on right Lens edge. Preview first. Main scene shows Depth hovered and its inline adjustment visible. Bottom 3 evenly spaced closeups of SAME node and same breakout geometry "At rest", "Hover or focus", "Adjusting". A single-control connected input editor labeled "Depth range" with dual-ended tiny track and values "10%" and "35%"; adjusted version "10%" and "22%". This is a proposed range UI, not a source-signal slider. The incoming cable must continue to the SAME socket visible at the same relative position in every state; the control extension opens perpendicular to or alongside anchor, never shifts socket. Keep main node and breakout visibly unified. Show no controls on any other node while this breakout is hovered. Small footer "One correction. Everything else stays put." No extra paragraphs or tiny filler text. Generous whitespace and crisp UI-sized typography, enough closeup scale to judge design. Exactly one sheet, no side-by-side whole proposals.Title "03 / Soft neck". Subtitle "A quiet control carried by the connection." Organic but unequivocally 2D software, not literal physical object. Main nodes dark compact squircle rectangles with very subtle soft surface gradient, preview vivid within nearly invisible rim. Input socket connects to housing by a SHORT tapered flat extension with a curved silhouette, same surface fill, like a natural neck 20-30 pixels long, NOT a hose or glass pipe. Violet picture neck and amber numeric neck have tiny accents but body remains neutral. On hover the numeric neck widens slightly vertically into a thin horizontal control ribbon above the cable, continuous with body; fixed socket at outer LEFT end on unchanged anchor point. Range control sits on ribbon with short readable "Depth range", 10% and 35%, amber dual-handle track. Tiny notch accommodates cable so no overlap. At rest just quiet narrow neck and modulation icon. Modulation pulse is a soft light that fills a small rounded flat square, not a yellow orb object. This version has softer geometry and a hint of gloss than alternatives but no thick bevel or 3D lighting. Bottom closeups must show attachment convincingly without detached boxes.
